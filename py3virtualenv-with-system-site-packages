#!/bin/sh

dir="${1}"
shift 1

if [ -z "${dir}" ]
then
    1>&2 echo "usage:"
    1>&2 echo "  ${0} <directory> [<option>]"
    exit 1
fi

set -x

#exec virtualenv "$dir" -p python3 --system-site-packages "$@"
exec python3 -m virtualenv "$dir" -p python3 --system-site-packages "$@"
